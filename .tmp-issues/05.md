Goal: all imports resolve via `@/`.

Tasks:
- Vite + tsconfig paths: `@/*` -> `src/*`, `packages/*` -> `packages/*`.
- Remove `.tsx` suffix imports; add lint/codemod note.

Acceptance:
- `git grep "from '.*\\.tsx'" -- src` returns nothing.
- No unresolved alias imports.
